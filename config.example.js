// Copy to `config.js` (not committed) and set your key.
// Required Google APIs (Google Cloud Console):
// - Maps JavaScript API (for interactive 360 panorama)
// - Street View Static API (for metadata + static thumbnails/hero)
//
// IMPORTANT: This key will be exposed in the browser. Restrict it by HTTP referrer.
window.GOOGLE_MAPS_API_KEY = "PASTE_YOUR_KEY_HERE";

